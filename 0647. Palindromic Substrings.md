[647. Palindromic Substrings](https://leetcode.com/problems/palindromic-substrings/)

#medium 
#TwoPointers 
#dynamicProg 
[[- Medium Level]]
[[- Two Pointers]]
[[- Dynamic]]


Given a string `s`, return _the number of **palindromic substrings** in it_.

A string is a **palindrome** when it reads the same backward as forward.

A **substring** is a contiguous sequence of characters within the string.

**Example 1:**

**Input:** s = "abc"
**Output:** 3
**Explanation:** Three palindromic strings: "a", "b", "c".

**Example 2:**

**Input:** s = "aaa"
**Output:** 6
**Explanation:** Six palindromic strings: "a", "a", "a", "aa", "aa", "aaa".

**Constraints:**

- `1 <= s.length <= 1000`
- `s` consists of lowercase English letters.


# Two Pointers
```python
class Solution:
    def countSubstrings(self, s: str) -> int:
        count = 0
        
        def expand(left, right):
            res = 0
            while left >= 0 and right < len(s) and s[left] == s[right]:
                res += 1
                left -= 1
                right += 1
            return res
            
        for i in range(len(s)):
            count += expand(i, i)     # Odd
            count += expand(i, i + 1) # Even
            
        return count
```

# Dynamic

```python
class Solution:
    def countSubstrings(self, s: str) -> int:
        n = len(s)
        if n == 0:
            return 0
            
        count = 0
        # dp[i][j] will be True if the substring s[i:j+1] is a palindrome
        dp = [[False] * n for _ in range(n)]
        
        # Base case: All single characters are palindromes
        for i in range(n):
            dp[i][i] = True
            count += 1
            
        # Base case: 2-character substrings
        for i in range(n - 1):
            if s[i] == s[i + 1]:
                dp[i][i + 1] = True
                count += 1                
        
        # General case: Substrings of length 3 and greater
        for length in range(3, n + 1):
            for i in range(n - length + 1):
                j = i + length - 1
                
                # Check characters at ends and if the inner substring is a palindrome
                if s[i] == s[j] and dp[i + 1][j - 1]:
                    dp[i][j] = True
                    count += 1                

        return count
```

