[139. Word Break](https://leetcode.com/problems/word-break/)

[[- Medium Level]]
[[- Dynamic]]
[[- String]]

#medium 
#dynamicProg 
#string 

Given a string `s` and a dictionary of strings `wordDict`, return `true` if `s` can be segmented into a space-separated sequence of one or more dictionary words.

**Note** that the same word in the dictionary may be reused multiple times in the segmentation.

**Example 1:**

**Input:** s = "leetcode", wordDict = ["leet","code"]
**Output:** true
**Explanation:** Return true because "leetcode" can be segmented as "leet code".

**Example 2:**

**Input:** s = "applepenapple", wordDict = ["apple","pen"]
**Output:** true
**Explanation:** Return true because "applepenapple" can be segmented as "apple pen apple".
Note that you are allowed to reuse a dictionary word.

**Example 3:**

**Input:** s = "catsandog", wordDict = ["cats","dog","sand","and","cat"]
**Output:** false

**Constraints:**

- `1 <= s.length <= 300`
- `1 <= wordDict.length <= 1000`
- `1 <= wordDict[i].length <= 20`
- `s` and `wordDict[i]` consist of only lowercase English letters.
- All the strings of `wordDict` are **unique**.

```python
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        dp = [True] + [False]*len(s)

        for i in range(1,len(s)+1):
            for w in wordDict:
                start = i - len(w)
                if start>=0 and dp[start] and s[start:i]==w:
                    dp[i] = True
                    break
        
        return dp[-1]
```

# Approach

I think the simplest way to find words in wordDict is to exapand range one by one.

```javascript
Input: s = "leetcode", wordDict = ["leet","code"]
```

For instance,

```javascript
l in wordDict → No
le in wordDict → No
lee in wordDict → No
leet in wordDict → Yes, we have "leet" in wordDict
c in wordDict → No
co in wordDict → No
cod in wordDict → No
code in wordDict → Yes, we have "code" in wordDict 
```

---

◽️ Quesiton

How can we start from `c` to find a new word after we found "leet"?

---

My strategy is to have `true` and `false` in each position.

```csharp
   l e e t c o d e
[T,F,F,F,F,F,F,F,F]
```

You'll see why we have `T` at index `0` later. Anyway, when we find one of target word, we change `F` to `T`.

In this case, when we are at `t`, we will have `leet` which is one of target words in `wordDict`, so change `F` to `T` at index `4`.

```csharp
   l e e t c o d e
[T,F,F,F,T,F,F,F,F]
```

And when we have `e` at the last index, we will have `code` which is in `wordDict`, so change `F` to `T` at the last index.

```csharp
   l e e t c o d e
[T,F,F,F,T,F,F,F,T]
```

Now we reach the end of `string` and `array`. Check the last value of the array, then return it.

```python
return True
```

Let me answer the question above. Basically we will create range like `l`, `le`, `lee` and `leet`...so we need to start point and current position.

A formula for start position is

```sql
start = current position - length of word in wordDict
```

For instance, now we are at index `4` and fount `t`. Start point should be

```sql
start = current position - length of word in wordDict
= 4 - length of `leet`(= 4)
= 0
```

Check index `0` in the array. That is `true`, **that means we found the target word at index `0`. In this case, `""` (empty string). until index 0, we comfirm that they were already valid. That's why we can focus from next position.**

Same thing will happen at the last index.

start poisition is

```sql
start = 8 - 4
= 4
```

**Check index `4` and that is `true`, so we understad until index `4`, we found valid word, so focus on the next position.**

Let's see how it work!

```javascript
Input: s = "leetcode", wordDict = ["leet","code"]
```

First of all, create `true` and `false` array

```csharp
   l,e,e,t,c,o,d,e
[T,F,F,F,F,F,F,F,F]
```

We know that index `0` is `true`, so we can start from index `1`.

```csharp
   l,e,e,t,c,o,d,e
[T,F,F,F,F,F,F,F,F]
   i
```

Let's calculate start point with words in `wordDict`.

```sql
start = 1 - 4(= length of leet)
= -3

start = 1 - 4(= length of code)
= -3
```

Both cases are negative, so we can't create range, we don't do anything.

Next, let me speed up until index `4`.

```sql
   l,e,e,t,c,o,d,e
[T,F,F,F,F,F,F,F,F]
     i

start = 2 - 4(= length of leet)
= -2

start = 2 - 4(= length of code)
= -2
------------------------------------
   l,e,e,t,c,o,d,e
[T,F,F,F,F,F,F,F,F]
       i

start = 3 - 4(= length of leet)
= -1

start = 3 - 4(= length of code)
= -1
------------------------------------
   l,e,e,t,c,o,d,e
[T,F,F,F,F,F,F,F,F]
         i

start = 4 - 4(= length of leet)
= 0
→ True
```

Now we check index `0` and it's `True`.

Let's compare strings,

```sql
if s[start:i] == word in wordDict
= s[0:4] = "leet" 
= "leet" == "leet"
= True

   l,e,e,t,c,o,d,e
[T,F,F,F,T,F,F,F,F]
         i
```

Let's continue.

```sql
   l,e,e,t,c,o,d,e
[T,F,F,F,T,F,F,F,F]
           i

start = 5 - 4(= length of leet)
= 1
→ False

start = 5 - 4(= length of code)
= 1
→ False
-----------------------------------
   l,e,e,t,c,o,d,e
[T,F,F,F,T,F,F,F,F]
             i

start = 6 - 4(= length of leet)
= 2
→ False

start = 6 - 4(= length of code)
= 2
→ False
-----------------------------------
   l,e,e,t,c,o,d,e
[T,F,F,F,T,F,F,F,F]
               i

start = 7 - 4(= length of leet)
= 3
→ False

start = 7 - 4(= length of code)
= 4
→ False
-----------------------------------
   l,e,e,t,c,o,d,e
[T,F,F,F,T,F,F,F,F]
                 i

start = 8 - 4(= length of leet)
= 4
→ True

if s[start:i] == word in wordDict
= s[4:8] = "code" 
= "code" == "leet"
= False

start = 8 - 4(= length of code)
= 4
→ True

if s[start:i] == word in wordDict
= s[4:8] = "code" 
= "code" == "code"
= True
```

Change `F` to `T` at the last index.

```csharp
   l,e,e,t,c,o,d,e
[T,F,F,F,T,F,F,F,T]
                 i
```

Then we finish iteration. All we have to do is just to return the last value of the array.

```python
return True
```

Easy!😄  
Let's see solution codes!

---

I selected 5 good Dynamic Programming questions.

[](https://leetcode.com/link/?target=https%3A%2F%2Fyoutu.be%2Fumhzc6ohDpI)

---

# Complexity

- Time complexity: O(n∗m∗k)  
    `n` is length of input string.  
    `m` is number of words in `wordDict`  
    `k` is average size of substrings.
    
- Space complexity: O(n)
    

```python
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        dp = [True] + [False] * len(s)

        for i in range(1, len(s) + 1):
            for w in wordDict:
                start = i - len(w)
                if start >= 0 and dp[start] and s[start:i] == w:
                    dp[i] = True
                    break
        
        return dp[-1]
```

# Step by Step Algorithm

1. **Initialization**:
    
    - Create a list `dp` of size `len(s) + 1` with all elements set to `False`, except the first element which is set to `True`. This list will keep track of whether the substring `s[0:i]` can be segmented into words from `wordDict`.
    
    ```python
    dp = [True] + [False] * len(s)
    ```
    
2. **Outer Loop**:
    
    - Loop through each index `i` from `1` to `len(s)`. This loop iterates through each position in the string `s` to check if the substring `s[0:i]` can be segmented.
    
    ```python
    for i in range(1, len(s) + 1):
    ```
    
3. **Inner Loop**:
    
    - For each `i`, loop through each word `w` in `wordDict`.
    
    ```python
    for w in wordDict:
    ```
    
4. **Check Validity**:
    
    - For each word `w`, calculate the starting index `start` as `i - len(w)`.
    - Check if `start` is non-negative, meaning the word `w` can fit within the substring `s[0:i]`.
    - Check if the value `dp[start]` is `True`, meaning the substring `s[0:start]` can be segmented.
    - Check if the substring `s[start:i]` matches the word `w`.
    
    ```python
    start = i - len(w)
    if start >= 0 and dp[start] and s[start:i] == w:
    ```
    
5. **Update dp**:
    
    - If all conditions are met (i.e., `start` is non-negative, `dp[start]` is `True`, and `s[start:i]` matches `w`), set `dp[i]` to `True` and break out of the inner loop to move to the next index `i`.
    
    ```python
    dp[i] = True
    break
    ```
    
6. **Return Result**:
    
    - After completing the loops, return the value of `dp[-1]`, which indicates whether the entire string `s` can be segmented into words from `wordDict`.
    
    ```python
    return dp[-1]
    ```
    

---